---
import bgVideo3_mp4 from '../../assets/videos/bg-video-3.mp4';
import bgVideo3_mov from '../../assets/videos/bg-video-3.mov';
import bgVideo3_webm from '../../assets/videos/bg-video-3.webm';
---

<section id="hero" class="enorganico enorganico-hero">
  <div class="container-fluid px-0">
    <div class="row">
      <div class="col-12 position-relative before:position-absolute before:top-0 before:left-0 before:right-0 before:bottom-0 before:w-full before:h-full before:bg-black/75">
        <picture>
          <video webkit-playsinline="" playsinline="" autoplay="" muted="" loop="" class="enorganico-hero__video w-100 h-[100dvh] object-cover">
            <source src={bgVideo3_mp4} type="video/mp4">
            <source src={bgVideo3_webm} type="video/webm">
            <source src={bgVideo3_mov} type="video/mov">
          </video>
        </picture>
      </div>
    </div>
  </div>
</section>

<style lang="scss">
  .enorganico {
    &#hero {
      width: auto;
      max-width: 100vw;
      overflow-x: hidden;
      position: relative;
    }

    &-hero {
      height: auto;
      min-height: 100vh;
      max-height: 100vh;
      overflow: hidden;
      video {
        width: auto;
        min-width: 100%;
        height: 100vh;
        object-fit: cover;
      }

      &__content {
        position: absolute;
        top: 50%;
        left: 0;
        transform: translate(20%, -50%);
        text-align: center;
        z-index: 1;
      }
    }
  }
</style>